name: Nightly Hydrogen Allure

on:
  schedule:
    - cron: '0 6 * * 1-5' # Runs every day at 1:00 AM EST
  workflow_dispatch:

jobs:
  run-advantage-tests:
    runs-on: ubuntu-latest

    env:
      PASSWORD: ${{ secrets.PASSWORD }}

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps

      - name: Run Hydrogen tests
        id: run-tests
        continue-on-error: true
        run: npx playwright test tests/ --reporter=line,allure-playwright
        env:
          CI: true

      - name: Generate Allure report
        if: always()
        run: npx allure generate allure-results --clean -o allure-report --single-file

      - name: Summary
        if: always()
        run: |
          if [ "${{ steps.run-tests.outcome }}" = "success" ]; then
            echo "Tests passed."
          else
            echo "Tests failed. Allure report is still available as an artifact."
          fi
          echo "Allure report generated at $GITHUB_WORKSPACE/allure-report"
          echo "Download it from the Actions run artifacts section."

      - name: Upload Allure report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-report-${{ github.run_number }}
          path: allure-report
          if-no-files-found: warn
          retention-days: 14

      - name: Fail workflow if tests failed
        if: steps.run-tests.outcome != 'success'
        run: exit 1